{
  "governedRegions": ${jsonencode(governed_regions)},
  "centralizedLogging":{
    "accountId":"${logging_account_id}",
    "configurations":{
      "loggingBucket":{
        "retentionDays": ${log_retention_days}
      },
      "accessLoggingBucket":{
        "retentionDays": ${log_retention_days}
      }
    },
    "enabled":true
  },
  "securityRoles":{
    "enabled": true,
    "accountId":"${security_account_id}"
  },
  "accessManagement":{
    "enabled":false
  },
   "backup": {
    "enabled": false
  },
  "config": {
    "accountId": "${security_account_id}",
    "enabled": true,
    "configurations": {
      "loggingBucket": {
        "retentionDays": ${log_retention_days}
      },
      "accessLoggingBucket": {
        "retentionDays": ${log_retention_days}
      }
    }
  }
}
